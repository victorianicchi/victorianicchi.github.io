/** @type {import('next').NextConfig} */
const nextConfig = {
    // You can add custom config here if needed.
    // For GitHub Pages with a subpath, you might add a basePath, assetPrefix, etc.
  }
  
  module.exports = nextConfig
  